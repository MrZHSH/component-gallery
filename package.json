{
  "author": "Zhang.Sheng",
  "private":true,
  "workspaces": [
    "packages/*"
  ],
  "name": "component-gallery",
  "scripts": {
    "build": "lerna exec --parellel -- babel --root-mode upward sec -d lib --ignore **/*.story.js,**/*.spec.js",
    "bootstrap": "lerna bootstrap --use-workspaces",
    "test": "lerna run test",
    "new-version": "lerna version --conventional-commit --yes",
    "diff": "lerna diff"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "webpack": "^5.68.0"
  },
  "version": "1.0.1",
  "dependencies": {
    "axios": "^0.25.0"
  }
}
